# living-docs Skills

This repo is Chinese-only as of v2 (2026-07-09). See [README.md](README.md) for the authoritative list of skills, install instructions, and repository layout.

- **v2 (living-docs)**: two documents (project doc + module map) maintained by one habit ("update the doc of whatever module you touch") plus git-anchored drift reconciliation. The suite is `docs-init`, `docs-sync`, `docs-acceptance`; six standalone skills (personal-style, git-commit, writing-plexus-notes, using-{cursor,claude,codex}-cli) ship alongside it — nine in total.
- **v1 (modular-programming)**: the bilingual 10-skill governance suite is frozen read-only in [`legacy/`](legacy/README.md), git tag `modular-v1-frozen`. `./install.sh en` is retired.

An English edition may be translated from the stable Chinese source if demand appears; no mirrored maintenance.
