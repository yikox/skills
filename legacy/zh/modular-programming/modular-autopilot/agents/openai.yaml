interface:
  display_name: "Modular Autopilot"
  short_description: "在一次接入确认后自主执行已接受的 L2/L3 architecture patch 或可选 proposal"
  default_prompt: "用 $modular-autopilot 对已接受并评审过的 L2/L3 architecture patch 或可选 proposal 做接入审查，然后规划、执行、验证，并带决策日志收尾。"
