interface:
  display_name: "PM Audit Memory"
  short_description: "Audit PM schema, roadmap, and archives"
  default_prompt: "Use $pm-audit-memory to audit project memory for stale tasks, schema drift, roadmap drift, lifecycle mismatches, archive candidates, and missing design links."
