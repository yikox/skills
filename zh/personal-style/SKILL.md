---
name: personal-style
description: 个人编码风格约定，独立于任何工作流。Use when 处理任何开发相关请求——写新代码、修改代码、设计接口、重构、评审、清理，以及下载依赖、工具、数据或模型；动手之前先加载本技能并全程遵循，所有开发请求都适用，无例外。中文触发词：个人风格、编码风格、我的风格、风格约定。
---

# 个人编码风格（Personal Style）

用户的个人编码风格约定。处理任何开发任务时全程遵循；与具体工作流无关，和其他技能叠加使用（其他技能管流程，本技能管品味）。

## 风格一：模块化，接口少而简单

- 任何新代码先想清楚它属于哪个模块、对外暴露什么接口，再写实现。
- 组件保持模块化，按关注点分离职责；不要为了形式上的分层把能运行的闭环拆散。
- 接口以"少而简单"为准：
  - 优先扩展或复用已有接口，而不是新增接口。
  - 一个模块对外暴露的入口保持最小集合；内部辅助函数、中间层不导出。
  - 参数简单直接；不为"将来可能用到"预留参数、配置项或抽象层。
- 如果一个改动会让模块对外接口变多、变复杂，先停下来向用户说明并给出更简单的替代方案。

## 风格二：整洁第一，不留无用之物

整洁是第一位的。完成任务时，工作区里只应留下两类东西：**仍然有效的**和**这次交付且需要长期维护的**。其余一律删除：

- 被替代或不再被引用的旧代码、旧函数、旧文件。
- 过程中的中间产物：临时脚本、调试输出、实验文件、一次性数据。
- 废弃的脚本和配置；注释掉的大段代码。
- 已被实现或新决策取代、与当前事实不符、没有长期读者的过期文档。
- 一次性计划、阶段报告、草稿、交接说明等临时文档。

文档也必须服从这条规则：

- 新建文档前，先确认它有长期读者、明确职责和唯一存放位置；否则只在对话中说明，或放进 scratchpad / 系统临时目录。
- 改动让现有文档过期时，在同一任务内更新、合并或删除；不保留多个事实源，也不靠标注“已废弃”把旧文档继续留在项目里。
- 流程确实需要临时文档时，从一开始就放在项目外；任务完成后删除。git 历史负责追溯，不用工作区保存过程遗迹。

| 想法 | 现实 |
|------|------|
| "先留着以防万一" | git 历史就是"以防万一"。删。 |
| "注释掉比删掉安全" | 注释掉的代码是噪音，且很快过时。删。 |
| "临时脚本以后可能还用" | 真可复用就整理进项目并纳入版本管理；否则删。 |
| "旧文档也许还能当参考" | 与当前事实冲突的参考比没有更糟；历史去 git 里找。删。 |
| "不是我创建的，不敢删" | 不确定归属时列出来问用户，但不要默默留下。 |

## 风格三：实现求简，架构求远

- 选择能满足当前需求的最简单实现；不做预防性抽象，不增加多余的配置层。
- 不保留向后兼容：过时的接口和实现直接删除，不加兼容层、不写 migration、不留 fallback。若直接删除会影响外部消费者或持久数据，先停下来向用户确认处理范围，不要自行偷偷加兼容方案。
- 架构决策要面向长期目标，不接受“先这样、以后再换”的临时方向；当前实现可以小，但不能故意选一条注定要推倒重来的路。

## 风格四：先跑通最小端到端闭环

- 系统分层较长时，先实现从输入到输出的最小端到端版本，再按当前需求逐层增加能力。
- 每次扩展都保留已有的可运行闭环；绝不为了尚未完成的复杂度拆掉能跑的东西。

## 风格五：先复用，再选成熟方案

- 动手前先检查项目已有依赖和工具能做什么；确认现有能力不足后，才考虑新增包或自行实现。
- 优先使用成熟、有人维护的库；没有明确理由，不自己重写已有能力。
- 先看成熟产品如何解决同类问题，优先采用已验证的模式；不要从零发明已有解法。

## 风格六：下载先测速，慢则换源

- 下载依赖、工具、数据或模型前，先用短时探测、元数据或初始进度估算速度与完成时间；小文件不为测速增加多余步骤。
- 如果速度持续过低或预计完成时间明显不合理，不要原地等待：
  1. 查找可信且更快的镜像、区域节点或等价分发渠道。
  2. 切换后重新探测速率，选择可靠且实际更快的来源。
  3. 例如 `pip` 可尝试阿里云等镜像；大模型可优先检查魔搭社区是否提供相同模型。
- 换源不能牺牲正确性与安全性：核对包名、版本、模型 revision 和文件是否一致；有校验和或签名时执行验证；不使用来源不明的镜像。
- 如果替代来源要求新增凭据、接受额外条款，或无法确认内容等价，先向用户说明并征得同意。

## 风格七：本地 Git 与 gh 默认使用外部终端

- 运行 `gh` 或涉及本地仓库、远端、索引、本地凭据、keyring、远端网络、hooks、`.git` 元数据的 `git` 操作时，默认使用外部终端（`require_escalated`/非沙箱）；不要在 Codex 沙箱中执行。包括 `status`、`diff`、`add`、`commit`、`worktree`、`remote`、`fetch`、`pull`、`push`、`clone` 等。
- 普通代码和文本读写继续在沙箱中进行。
- 外部终端不可用时，报告阻塞；不要把 token 写入命令或日志；不要用 `--no-verify` 绕过 hooks。
- 只有用户或环境明确要求时，才在沙箱中执行上述操作。

## 风格八：测试材料默认不入库

- 测试代码、测试脚本、fixture、快照、覆盖率报告、录屏/截图、日志、临时数据及构建/运行产物，默认只用于临时验证，不得暂存或提交到代码仓库。
- 尽量一开始把临时测试材料放在系统临时目录或项目已忽略目录。
- 只有用户明确要求保存，或任务明确把某项测试定义为需要长期维护的回归测试/交付物时，才将其作为正式代码入库；真正约定的长期测试不得误删。
- 任务结束时若工作区仍有本次生成且未明确保留的测试代码或测试产物，先列出并询问用户是否删除；用户决定前不得提交，也不得擅自删除。

## 收尾自检

交付前自问：

1. 新增/变更的对外接口数量是否为完成需求的最小值？
2. 项目目录里是否还有临时文件、临时文档、废弃代码或调试残留？
3. 现有文档是否仍与实现一致，过期或重复的文档是否已经更新、合并或删除？
4. 是否保留了不必要的兼容层、migration、fallback、预防性抽象或多余配置？
5. 是否先检查了现有依赖和成熟模式，并保持最小端到端闭环可运行？
6. 本次若有下载，是否先判断了速度，并在过慢时尝试了更可靠高效的来源？
7. 本次生成的临时测试材料是否默认放在系统临时目录或项目已忽略目录，且未明确长期保留的材料是否已列出并询问用户，用户决定前是否未暂存或提交？

任一答案不满意，先处理再交付。
