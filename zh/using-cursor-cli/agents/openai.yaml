interface:
  display_name: "使用 Cursor CLI"
  short_description: "通过 Cursor Agent CLI 低带宽执行分析、规划、复核与隔离开发任务"
  default_prompt: "使用 $using-cursor-cli 让本地 Cursor Agent CLI 承担主要推理与执行；完整日志只写临时文件，默认等待完成唤醒，仅返回最终摘要和窄验收结果。"
