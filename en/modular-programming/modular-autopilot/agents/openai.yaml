interface:
  display_name: "Modular Autopilot"
  short_description: "Execute accepted L2/L3 architecture patches or optional proposals with one intake confirmation"
  default_prompt: "Use $modular-autopilot to intake-review an accepted and reviewed L2/L3 architecture patch or optional proposal, then plan, execute, verify, and close out with a decision log."
